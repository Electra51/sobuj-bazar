import { Container, Typography } from '@mui/material';
import React from 'react';

const ThirdSection = () => {
    return (
        <Container>
            <Typography style={{
                fontWeight: '600',
                fontSize: '1.2rem',
                
            }}>
            Trendy Products
            </Typography>
        </Container>
    );
};

export default ThirdSection;